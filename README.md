# 未来浏览器 — 轻量 iOS 浏览器（Flutter + WKWebView 混合开发）

Flutter 3.47.x + [webview_flutter](https://pub.dev/packages/webview_flutter) 4.14.x 构建的 iOS 浏览器（工程名 NewWeb）。
iOS 底层为系统 WKWebView（App Store 对浏览器的强制要求），Flutter 提供壳 UI 与业务逻辑，Swift 原生层提供下载、内容拦截、中文菜单等系统能力。

- **目标系统**：iOS 15.0+
- **包名**：com.newweb.newweb
- **应用名**：未来浏览器

## 功能特性

### 浏览核心
- **多标签页**：最多 8 个，网格切换 / 关闭 / 新建，后台标签保活
- **手势导航**：左边缘右滑返回、右边缘左滑前进（与页面滚动共存）
- **下拉刷新**、前进 / 后退 / 刷新 / 首页 / 加载进度条
- **地址栏**：网址 / 搜索词自动识别（非网址走所选搜索引擎，支持百度 / 必应 / Google）
- **无痕模式**：不记录历史；退出无痕时清空全部网站数据
- **100% 汉化**：全部界面文案 + 系统组件中文本地化 + 长按菜单中文化

### 网页翻译
- **整页翻译**：收集页面可见文本 → 翻译 → 按原文位置回填，可一键恢复原文
- **手动翻译**（长按菜单 / 更多菜单「翻译此页」）与**自动翻译**（自定义网址白名单，精确或子域匹配，加载完成后自动触发）
- **在线 / 离线双模式**：在线走 Google → MyMemory → 腾讯云三级降级；离线仅走内置本地词库
- 翻译模式可在设置页切换（自动 / 在线 / 离线）

### 下载管理
- 原生 URLSession 下载，支持**暂停 / 断点续传 / 取消**，落盘 Documents/Downloads，重名自动去重
- 网页内点击常见文件后缀链接（zip / apk / ipa / dmg / pdf 等）**自动接管下载**
- 下载管理页：进行中任务（进度条 / 暂停 / 续传 / 取消）+ 已完成列表（原生预览 / 分享 / 删除）
- **手动输入链接下载**兜底（无后缀 CDN 链接可用长按菜单「下载链接」或手动粘贴）

### 广告拦截（原生 WKContentRuleList）
七大模块：
1. **资源拦截 block**：80+ 广告域名黑名单（doubleclick、googlesyndication、google-analytics、facebook、umeng、cnzz、home.baidu、字节广告域等），正则 url-filter 匹配，原生编译注入
2. **DOM 元素隐藏 css-display-none**：CSS 选择器隐藏同域投放广告
3. **Cookie 与追踪拦截 block-cookies**：拦截 GA / FB Pixel 等追踪器
4. **弹窗拦截**：拦截 popup 类型请求 + JS window.open 守卫
5. **DNS 层过滤**：生成 AdGuard DNS 配置描述文件（设置页 → 安装 AdGuard DNS），系统级域名解析拦截
6. **阅读器模式**：内置正文提取（按 p 标签 / 文本长度打分，剔除广告与评论区），原生阅读页渲染，字号可调
7. **反广告检测对抗**：豁免站点白名单（ignore-previous-rules），命中站点跳过全部拦截，防止"请关闭广告拦截"提示

### 缓存管理（四级）
- **L1 网页缓存**：沙盒 Caches 与网络缓存（clearHttpCache）
- **L2 网络缓存**：WebView 磁盘缓存（diskCache）
- **L3 Cookie 与站点数据**：Cookie / localStorage / IndexedDB / WebSQL（显示记录数）
- **L4 全部数据**：网站数据 + 离线页面 + 历史记录一键清空
- 入口：更多菜单「缓存管理」或 设置 → 存储

### 其他
- 书签（默认书签 / 添加 / 删除，SQLite 存储）、历史记录（自动记录 / 相对时间 / 清空）
- 离线页面：整页保存（HTML + 内联 CSS/图片），离线可读
- 长按链接中文菜单：复制 / 翻译此页 / 下载链接
- JS Bridge 通道（Web→App / App→Web）

## 更新日志

### v1.0.45（最新 · 移除书签联想与下拉刷新）
- **移除地址栏书签联想**：删除输入联想下拉面板及相关逻辑，地址栏仅保留输入/搜索/域名高亮
- **移除下拉刷新功能**：删除手势层下拉刷新指示器、刷新回调及相关状态，保留边缘手势与顶部轻下拉聚焦地址栏

### v1.0.44（精简地址栏功能）
- **移除地址栏历史记录联想**：输入联想仅匹配书签，不再显示历史记录
- **移除剪贴板网址检测弹窗**：删除回前台剪贴板检测、顶部提示条及设置开关
- **移除长按地址栏菜单**：删除长按弹出的粘贴/复制/全选/清除菜单

### v1.0.43（修复地址栏点击无反应）
- **修复单击地址栏无反应无法进入编辑状态**：浏览态点击时先切换到输入态显示 TextField，下一帧再请求焦点（原实现因 focusNode 无绑定输入框导致无法聚焦）
- **修复点击页面区域地址栏不失焦**：GestureLayer 新增 onTapPage 回调，轻触页面非边缘区域时自动收起键盘和联想下拉

### v1.0.42（地址栏优化修复）
- **修复一键清空按钮不实时更新**：监听 controller 文本变化，输入/删除时即时显示/隐藏清空按钮
- **修复加载中锁头与进度圈重叠**：加载中只显示蓝色进度圈，不再同时显示刷新图标
- **修复浏览态点击地址栏显示旧内容**：聚焦时自动同步 controller.text 为当前 URL 并全选
- **修复长按菜单与系统选择菜单冲突**：移除外层 GestureDetector，仅浏览态域名区域支持长按菜单
- **修复输入态滑动误触发前进后退**：输入态禁用地址栏横向滑动手势
- **修复联想下拉挤压页面布局**：改为 Stack 浮层覆盖在 WebView 上方，不再挤压下方内容
- **修复网址判断误判**：更严格的域名判断（需符合域名后缀格式），"1.2"等不再误判为网址
- **修复剪贴板检测频繁弹窗**：30秒冷却时间，避免频繁触发系统剪贴板读取提示
- **修复滚动回调频繁 setState**：200ms 节流，减少性能消耗
- **新增下拉地址栏快速聚焦**：页面顶部轻下拉（30-80pt）自动聚焦地址栏，超过80pt触发下拉刷新
- **地址栏聚焦时禁用自动隐藏**：避免输入时地址栏意外收起

### v1.0.41（地址栏全面增强）
- **智能自动补全**：输入时实时联想历史记录+书签，匹配结果下拉展示，书签优先，历史可单条删除
- **域名高亮**：浏览态地址栏显示协议灰色、主域名加粗黑色、路径/参数浅灰，层次清晰
- **一键清空**：输入态右侧显示 × 按钮，一键清空地址栏内容
- **长按增强菜单**：粘贴并跳转（剪贴板是URL时高亮）、粘贴、复制链接、全选、清除地址
- **剪贴板网址检测**：回到前台时自动检测剪贴板中的网址，顶部蓝色提示条可一键打开（设置可开关）
- **地址栏状态动态变化**：输入态蓝色边框聚焦，浏览态域名高亮，加载中锁头变旋转动画
- **左右滑动前进后退**：在地址栏区域左滑/右滑可前进/后退（需有历史记录）
- **搜索引擎快速切换**：输入态左侧调谐图标可临时切换百度/必应/Google（本次搜索生效）
- **搜索关键词智能判断**：含 . 或 http 开头判为网址，否则走搜索引擎搜索
- **一键复制链接**：长按地址栏菜单中复制当前页链接，Toast 提示
- **地址栏自动隐藏**：向下滚动网页时隐藏地址栏，向上滚动时显示（设置可开关，默认关闭）
- **设置页新增**：剪贴板网址检测、自动隐藏地址栏两个开关

### v1.0.40（菜单换位 + 长截图重构防闪退白边）
- **菜单换位**：更多菜单第一行第四个改为「设置」，「离线页面」移至「其他」分组
- **长截图重构（修复卡住/闪退/白边）**：
  - 限制最大截图高度 10000pt、最多 25 段，超高页面自动等比缩小，杜绝内存爆炸闪退
  - 改用 JPEG（质量 0.85）输出，体积大幅减小
  - 每段截图前等待 0.28s 渲染稳定，`afterScreenUpdates` 消除顶部白边覆盖信号栏
  - 截图期间禁用弹性滚动，结束后双重恢复原始滚动位置
  - 分段失败自动跳过，`autoreleasepool` 及时释放内存
  - 新增 30 秒超时保护和持续加载进度框，不再假死
- **PDF 导出**：同步增加加载进度框

### v1.0.39（菜单修复 + PDF导出 + 源码增强 + 移除主屏幕）
- **更多菜单修复**：固定半屏高度，内容区域独立滚动，解决菜单到顶滑不下来的问题
- **导出网页 PDF**：原"导出长图"改为真正的 PDF 导出，使用 WKWebView 原生 PDF 渲染，生成后自动弹出系统分享
- **截长图保存修复**：保存相册添加完成回调，记录保存结果，解决偶尔保存失败的问题
- **源码查看增强**：
  - 标题显示网站域名而非"网页源码"
  - 新增 iOS 系统分享按钮，可分享源码文本
  - 新增关键词查找功能，支持上下跳转匹配结果
- **移除添加到主屏幕**：移除 WebClip 功能及全部相关代码（Swift/Dart/菜单入口）

### v1.0.38（网页长截图 + 源码查看 + 添加主屏幕 + 菜单完善）
- **网页长截图**：滚动拼接整页网页，生成高清长图，支持保存相册和分享
- **导出网页长图**：高清渲染导出整页网页图片
- **查看网页源码**：内置深色代码风格源码查看器，支持复制全部和下拉刷新
- **添加到主屏幕**：生成 WebClip 配置文件，一键添加网页快捷方式到 iPhone 主屏幕
- **更多菜单完善**：Chrome 三段式菜单新增 4 个网页操作入口，统一图标和间距
- **相册权限**：新增 NSPhotoLibraryAddUsageDescription 权限描述

### v1.0.37（密码本自动填充 + UI精致度优化）
- **密码本功能**：
  - 本地存储网站账号密码，SQLite 持久化，不上传任何服务器
  - 主密码保护：设置主密码后访问密码本需验证
  - 自动填充：检测到登录表单时自动匹配域名并填充用户名密码
  - 保存提示：登录表单提交时弹出底部弹窗，一键保存/更新密码
  - 密码本管理页：搜索、添加、编辑、删除，域名首字母图标
  - 自动填充开关：可在密码本页一键开启/关闭
  - 入口：设置→密码与隐私→密码本，更多菜单→密码本
- **UI精致度优化**：
  - 设置页新增「密码与隐私」分组
  - 更多菜单新增密码本入口
  - 下载页空状态优化：图标+提示文字

### v1.0.36（网址栏小锁头扩展功能）
- **锁头多状态动态渲染**：
  - 🟢 绿色锁：HTTPS 安全连接
  - 🟡 黄色警告：HTTP 明文 / 混合内容
  - 🔴 红色风险：证书无效 / 危险网站
  - 加载中显示旋转加载图标
  - 标签锁定时右上角叠加橙色小锁角标
- **点击锁头弹出底部安全面板**：
  - 安全信息区：加密协议、证书状态、DNS过滤、广告拦截统计
  - 站点权限管控：相机/麦克风/位置/相册/弹窗，每项 询问/允许/拒绝
  - 快捷操作：锁定标签开关（与标签长按锁定双向同步）、强制HTTPS升级、清除本站Cookie和缓存
  - 风险页面显示红色警告提示
- **站点配置持久化**：每个域名独立保存权限和强制HTTPS设置，App重启后仍有效

### v1.0.35（标签主域名显示 + 编辑网址 + 锁定功能 + 地址栏重构）
- **标签显示优化**：
  - 默认显示网站主域名（如 baidu.com，自动去掉 www. 前缀）
  - 用户自定义标题优先显示，留空恢复主域名
- **长按菜单增强**：
  - 编辑标题：自定义标签名称，留空恢复默认
  - 编辑网址：修改标签 URL 并立即加载
  - 锁定标签 / 解锁标签：锁定后不可关闭
- **标签锁定功能**：
  - 锁定标签显示橙色锁图标 🔒
  - 锁定后关闭按钮隐藏，长按菜单不显示关闭选项
  - 锁定状态持久化保存，重启后仍有效
- **地址栏样式重构**：
  - iOS 风格：浅灰背景 + 白色输入框 + 圆角 10 + 轻微阴影
  - HTTPS 网站显示绿色锁图标，普通页面显示搜索图标
  - 刷新按钮改为蓝色 #007AFF
  - 整体更紧凑美观

### v1.0.34（顶部标签栏 + 长按编辑/删除）
- **新增顶部标签栏**：
  - 地址栏下方固定标签栏，高度 34px（不占空间）
  - 横向滚动显示所有标签，活动标签白色背景+蓝色边框高亮
  - 点击标签即时切换全屏浏览窗口
  - 右侧「+」按钮快速新建标签
  - 每个标签右侧带关闭按钮 ✕
- **长按标签操作菜单**：
  - 编辑标题：弹窗输入自定义标签标题
  - 关闭标签：关闭后自动切换到另一个标签
  - 全部关闭时自动新建一个标签
- **与现有系统打通**：复用 TabManager 多标签管理、快照、历史记录等全部功能

### v1.0.33（修复前台自动检测更新不触发）
- **缩短检查间隔**：从 1 小时缩短到 5 分钟，回到前台更快检测到更新
- **首次安装立即检查**：从未检查过的设备不受间隔限制，启动后立即检测
- **全链路调试日志**：
  - 自动检测开关状态、距上次检查时间、是否因间隔跳过
  - GitHub API 请求状态、最新版本号、是否有 IPA 资产
  - 当前版本号、版本比较结果、是否已是最新
  - 用户跳过版本记录、是否弹出更新提示、异常信息
  - 调试日志页面新增「更新」模块过滤，可单独查看更新相关日志

### v1.0.32（App 内下载 IPA，不再跳转 Safari）
- **更新下载改为 App 内完成**：
  - 点击「立即更新」后在 App 内后台下载 IPA，不跳转 Safari
  - 下载完成显示文件大小，自动唤起全能签/TrollStore 等签名工具等待确认安装
  - 下载失败时提示重试，不再跳转浏览器
- **重构更新检测逻辑**：
  - 抽取通用 `_fetchLatestRelease` 方法，改用循环获取 IPA 下载链接（更可靠）
  - 设置页「检查更新」和前台自动检测共用同一套逻辑
  - 有 IPA 资产时显示「立即更新」，无资产时才显示「前往下载」

### v1.0.31（修复检查更新失败 + DNS 配置文件安装方式优化）
- **修复检查更新失败**：仓库改为公开，App 内未认证调用 GitHub API 不再返回 404
- **DNS 配置文件安装优化**：
  - 改用 `UIDocumentInteractionController` 弹出打开方式菜单
  - 用户可选择「Safari」打开，Safari 会提示下载配置文件
  - 随后到「设置→通用→VPN与设备管理」完成安装
  - 更新弹窗文案，明确引导操作步骤

### v1.0.30（前台自动提醒更新 + 一键下载安装）
- **前台自动检测更新**：
  - App 启动 3 秒后自动检查，回到前台时自动检查
  - 检查间隔 1 小时，避免频繁请求
  - 用户跳过的版本不再提示
- **更新提示弹窗**：发现新版本时自动弹出，显示最新版本号和更新内容
  - 【立即更新】：后台下载 IPA → 自动唤起全能签等签名工具等待确认安装
  - 【稍后提醒我】：记录跳过该版本，不再提示
- **设置开关**：设置→关于→「打开App自动检测更新」，用户可关闭
- **CI 自动发布 Release**：每次构建完成后自动创建 GitHub Release 并上传 IPA 到 Assets，App 内可直接下载

### v1.0.29（检查更新 + 项目仓库跳转 + Release 发布）
- **设置页新增检查更新**：蓝色字体，点击后调用 GitHub API 获取最新 Release，比较版本号，有更新则弹窗显示更新内容并提供「前往下载」按钮
- **设置页新增项目仓库**：蓝色字体，显示仓库地址，点击直接在 Safari 中打开 GitHub 仓库
- **每次更新自动创建 GitHub Release**：包含版本号、更新内容、下载指引
- **原生新增 openWebURL**：在 Safari 中打开任意网页 URL

### v1.0.28（标签页上下眉还原 + DNS 一键安装）
- **多标签页上下眉还原浅灰**：AppBar 和底部栏还原为浅灰 #F5F6F8，与白色卡片呼应，和黑色背景形成视觉对冲
- **DNS 配置文件一键安装**：
  - 新增「立即安装」选项，用 `UIApplication.shared.open` 直接打开 .mobileconfig，系统自动弹出设置应用完成安装
  - 保留「分享描述文件」选项作为备用
  - 修复分享面板看不到 Safari 的问题（Safari 只在分享 URL 时出现，分享文件不显示）

### v1.0.27（Chrome 风格更多菜单 + 标签页黑底白卡）
- **更多菜单改 Chrome 三段式风格**：
  - 顶部拖拽横条 + 常用功能网格（书签/历史/下载/离线，4列图标+文字）
  - 网页操作分组（添加到书签/翻译此页/阅读模式/保存离线页面）
  - 其他分组（缓存管理/设置）
  - 列表项右侧加箭头，圆角从 16 加大到 20
- **多标签页背景改黑色**：
  - Scaffold/AppBar/底部栏全部改为黑色/深灰
  - 卡片改为白色，与黑色背景形成鲜明对比（Chrome 风格）
  - 文字/图标颜色适配深色背景

### v1.0.26（版本号动态读取 + 书签/历史/广告拦截增强）
- **设置页版本号动态读取**：使用 package_info_plus 动态读取应用版本，不再写死，以后更新自动同步
- **书签管理增强**：
  - 新增搜索功能（标题/网址模糊匹配）
  - 新增编辑功能（修改标题和网址）
  - 长按弹出操作菜单（打开/编辑/分享/删除）
  - 右侧更多按钮替代删除按钮
- **历史记录增强**：
  - 按日期分组显示（今天/昨天/本周/月日/年月）
  - 新增搜索功能
  - 左滑删除单条记录
  - 组内显示具体时分
- **广告拦截增强**：
  - 开关开启时显示已启用规则总数（内置+自定义）
  - 自定义规则入口显示分类统计（拦截/隐藏/白名单数量）
  - 从自定义规则页返回后自动刷新统计
- **数据库**：新增 updateBookmark、deleteHistoryEntry 方法

### v1.0.25（全局调试日志 + 下载管理增强）
- **移除快照专用日志**：删除 SnapshotLogger 和快照调试日志页面，改为全局 DebugLogger
- **全局调试日志**：支持按模块过滤（通用/快照/下载/广告拦截/翻译/导航/原生），最多保留 500 条，支持复制/分享/清空，设置→关于→调试日志
- **下载管理增强**：
  - 已下载文件显示完成时间（今天显示时分，今年显示月日时分）
  - 文件类型图标（图片绿/视频紫/音频橙/文档蓝/压缩包棕/应用青/其他灰）
  - 下载完成全局弹窗提示（关闭/分享/打开），在浏览器主页和下载页均生效
  - 已下载文件按时间倒序排列

### v1.0.24（视图层级打印日志）
- 加完整视图层级打印日志，定位 WKWebView 位置

### v1.0.23（修复 findWebView 找不到 WKWebView）
- **修复快照找不到 WKWebView**：根因是单纯遍历 `window.subviews` 穿不透 Flutter 的 ViewController 容器（平台视图放在 childViewController 中）。改为从 `rootViewController` 开始遍历，同时穿透 `childViewControllers` 和 `presentedViewController`，确保能找到 Flutter 的 WKWebView
- 通过手机端日志精确定位：`findWebView 遍历 window[0] → 所有 window 遍历完毕，未找到 WKWebView`

### v1.0.22（快照彻底修复）
- **彻底修复快照写文件权限问题**：放弃 Swift 端写文件方案（Caches/tmp 目录均报权限错误），改为 Swift 直接将 PNG 数据 base64 编码返回 Dart，由 Dart 解码后写入 AppSupport 目录（Flutter 官方可写目录，绝无权限问题）
- 数据量约 72KB → base64 96KB，MethodChannel 传输无压力

### v1.0.21（快照根因修复）
- **修复快照完全不显示的根因**：Swift 端写文件到 `CachesDirectory/Snapshots/` 报权限错误（You don't have permission），导致截图成功但写文件失败返回 null。改用 `FileManager.default.temporaryDirectory`（iOS 一定可写），并将 `try?` 改为 do-catch 暴露错误
- 通过 v1.0.20 手机端日志功能精确定位：findWebView ✅ → takeSnapshot ✅ (72KB) → 写文件 ❌ 权限错误

### v1.0.20（手机端日志导出）
- 新增「快照调试日志」页面（设置 → 关于 → 快照调试日志），手机端直接查看日志
- 日志支持**复制全部**和**系统分享**，无需电脑即可导出日志发给开发者
- Swift 端日志通过 captureSnapshot 返回值带回 Dart，全链路日志统一收集到 SnapshotLogger
- 日志按成功（绿）/失败（红）高亮显示，最多保留 200 条

### v1.0.19（快照调试日志）
- 在快照完整链路添加 `[NW-Snapshot]` 调试日志：Swift 端（findWebView/takeSnapshot/写文件）、Dart 端（_refreshSnapshot/写AppSupport/卡片加载），每一步打印成功/失败和关键数据，用于精确定位快照不显示的根因

### v1.0.18（快照修复）
- **修复快照完全不显示**：根因是 Swift 端 `findWebView` 用 URL 匹配查找 WKWebView，Flutter 平台视图 URL 与 tab.url 不一致（重定向/尾斜杠）导致匹配失败，返回 nil。改为直接返回第一个可见的 WKWebView，不做 URL 匹配
- **修复截图时机**：打开标签切换页前先截图（无延迟，最多等 800ms），截图完成后再 push 页面，避免 WebView 被覆盖后截不到内容
- `_refreshSnapshot` 新增 `delay` 参数，页面加载完成后用 400ms 延迟等渲染稳定，即时截图用 0ms

### v1.0.17（快照优化）
- **快照缩略图优化**：Swift 端 `takeSnapshot` 使用 `WKSnapshotConfiguration.snapshotWidth = 180pt`，直接生成多标签卡片尺寸的缩略图，文件体积从全屏几百 KB 降到几十 KB，加载更快
- **截图时机优化**：点击多标签按钮时先触发当前页快照（后台异步不阻塞），确保标签切换页显示最新快照
- 快照持久化到 `AppSupport/snapshots/{tabId}.png`，App 重启后仍保留

### v1.0.16（回退 M9 稳定版）
- **回退到 M9 稳定版本**：移除 M10-M15 引入的重力感应、深色模式、翻译重构等不稳定功能，恢复 M9 稳定体验
- **工具栏加号改为 iOS 分享按钮**：点击调用系统分享面板，分享当前网页 URL 和标题
- **工具栏配色改为 iOS 风格**：可用状态显示 iOS 系统蓝（#007AFF），不可用状态显示灰色（#C7C7CC）

### v1.0.9
- **快照修复**：页面加载完成后延迟 400ms 截图（等渲染稳定），标签切换页优先读取磁盘快照文件，App 重启后快照仍保留
- **标签切换页底部栏改版**：左下角「修改」+ 中间蓝色加号（新建空白标签）+ 右下角「完成」
- **修改子菜单**：「选择标签页」进入批量选择模式；「关闭所有标签页」二次确认后清空
- **批量选择模式**：左上角「全选/取消全选」、右上角「完成」；卡片右上角蓝色圆圈选择指示器（选中显示白色对勾）；选中卡片蓝色边框高光
- **批量操作栏**：选择模式底部显示「关闭 N 个」「添加 N 个书签」，未选择时灰色禁用
- 删除卡片内「当前」文字，当前激活标签改用蓝色边框标识（对齐 Chrome 风格）

### v1.0.8
- **LRU 标签内存回收**：同时最多保活 6 个最近使用的 WKWebView，超出的标签自动休眠释放内存；切回休眠标签时重建 WebView 并恢复页面
- **快照磁盘持久化**：页面加载完成时截图写入沙盒稳定路径（按标签 ID 命名），App 重启后标签网格仍可显示上次浏览快照；关闭标签同步删除快照
- **滚动位置保存恢复**：离开标签时保存页面滚动偏移，重建/重启后自动恢复浏览位置
- **底部工具栏改版**：中间主页图标替换为加号（新建空白标签页 about:blank）；多标签图标改为 Chrome 风格圆角方框 + 实时标签数量
- 无痕模式不生成快照、不保存会话、不保存滚动位置

### v1.0.7
- **修复标签快照不显示**：改为原生写 PNG 文件 + URL 匹配标签，绕过大消息传输限制；页面加载完成 / 切换标签时自动更新
- **标签会话持久化**：退出应用后标签（URL / 标题 / 激活项）自动保存，下次启动恢复；无痕模式不保存不恢复
- **广告拦截自定义规则**：设置 → 广告拦截 → 自定义规则（拦截域名 / 隐藏元素 / 豁免站点 / 高级 JSON），变更即时重新注入，无需发版
  - 域名输入自动清洗：粘贴 `https://www.example.com/path` 自动去掉 https:// 前缀与路径尾缀，自动匹配所有子域
  - 豁免白名单已收编至自定义规则页（设置页旧数据自动迁移）
- 长按中文菜单修复：WebView 无原生 uiDelegate 时菜单也生效

### v1.0.6
- **下载增加弹窗确认**：网页内下载链接自动接管、长按菜单「下载链接」均先弹确认框，确认后才开始下载
- **多标签浏览快照**：标签切换页每个标签卡片显示最后浏览快照（原生 WKWebView 截图，页面加载完成 / 切换标签时自动更新）
- **build.yml 全面汉化**：工作流名称、任务、步骤、注释全部改为中文

### v1.0.5
- **修复下载拉起**：网页内点击下载链接（zip/apk/ipa/dmg/pdf 等常见后缀）自动接管为原生下载，不再被 WebView 当普通页面打开
- 下载管理页新增**手动输入链接下载**兜底入口
- 更多菜单可滚动（修复「下载管理」「设置」被裁剪不可见）；新增「缓存管理」快捷入口

### v1.0.4
- 修复更多菜单内容超出屏幕被裁剪、无法滚动的问题
- 更多菜单新增「缓存管理」快捷入口

### v1.0.3（M4）
- 下载管理（断点续传 / 暂停 / 取消 / 已完成列表）
- 网页翻译重构：整页翻译，手动 + 自动白名单，在线 / 离线双模式
- 缓存管理升级为四级（L1~L4）
- 广告拦截升级为原生 WKContentRuleList 七大模块
- 长按菜单汉化（复制 / 翻译此页 / 下载链接）；删除与原生选中贴窗重叠的选中翻译

### v1.0.2（M3）
- 离线整页保存 / 翻译三层降级 / 缓存管理 / JS 广告拦截 / 无痕模式 / 设置页

### v1.0.1（M2）
- 多标签 / 手势导航 / 书签 / 历史记录 / 100% 汉化

### v1.0.0（M1）
- 项目骨架 / 浏览核心 / 下拉刷新 / JS Bridge

## 目录结构

```
lib/
├── main.dart                        # 入口
├── app.dart                         # 应用根组件 / 主题 / 启动初始化
├── core/
│   ├── config/app_config.dart       # 首页、搜索引擎、UA、默认书签
│   ├── db/database_helper.dart      # SQLite（书签 / 历史）
│   ├── bridge/                      # JS Bridge（js_bridge / web_injections）
│   └── services/                    # 设置 / 翻译 / 广告拦截 / 下载 / 离线
├── features/browser/
│   ├── browser_screen.dart          # 主界面（菜单入口）
│   ├── webview_page.dart            # WebView 容器（导航 / 翻译 / 阅读器）
│   ├── download_page.dart           # 下载管理
│   ├── cache_manager_page.dart      # 四级缓存
│   ├── reader_page.dart             # 阅读器模式
│   ├── settings_page.dart           # 设置
│   └── widgets/                     # 地址栏 / 进度条 / 工具栏 / 手势层
└── native/
    └── native_bridge.dart           # MethodChannel + EventChannel

ios/Runner/
├── NativeBridge.swift               # 原生桥（清数据 / 下载 / DNS / 内容拦截）
├── DownloadManager.swift            # URLSession 下载（断点续传）
├── ContentBlockerManager.swift      # WKContentRuleList 编译注入
├── WebViewDelegateWrapper.swift     # 中文长按菜单（WKUIDelegate 包装）
└── assets/adblock_rules.json        # 广告拦截规则（Dart 侧）
```

## 云端构建（推荐，无需本地 macOS）

推送到 GitHub 后，`Build iOS IPA` 工作流自动构建（macOS runner + Flutter 3.47.2 + Xcode）。

- **无签名模式**（默认）：产物 `NewWeb-unsigned-ipa`，用 TrollStore / AltStore / Sideloadly / 爱思助手侧载
- **签名模式**：在仓库 **Settings → Secrets and variables → Actions** 配置以下 Secret 后自动切换：

| Secret | 内容 |
|---|---|
| `IOS_CERTIFICATE` | 你的 .p12 证书 base64 |
| `IOS_CERTIFICATE_PWD` | 证书密码 |
| `IOS_PROVISION_PROFILE` | .mobileprovision 描述文件 base64 |
| `KEYCHAIN_PASSWORD` | 任意临时钥匙串密码 |

base64 生成（macOS）：`base64 -i certificate.p12 | pbcopy`

> 证书 / 描述文件的 Bundle ID 必须与项目一致（`com.newweb.newweb`）；
> 构建完成后在 Actions 页面下载对应版本 IPA 产物。

## 本地开发

```bash
flutter pub get
flutter analyze
flutter test
# 构建需 macOS + Xcode：
cd ios && pod install
flutter build ios --release --no-codesign   # 无签名
```

## 版本管理

- **版本规则**：每次更新 `pubspec.yaml` 中 `version` 的版本号 +0.01（如 `1.0.4+1` → `1.0.5+1`），并同步本文件「更新日志」
- 应用名：`ios/Runner/Info.plist` 中 `CFBundleDisplayName` = 未来浏览器
- 打包模式：默认无签名 IPA（TrollStore 侧载）；配置证书 Secrets 后自动切换签名模式
